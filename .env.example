# Copy to .env and fill in your values.
# ---- Solana ----
# Solana RPC (default: devnet)
RPC_URL=https://api.devnet.solana.com

# Optional: watch this Solana address for every transaction (leave unset to only watch slot height)
# WATCH_ADDRESS=YourSolanaPublicKeyHere

# Optional: alert when Solana slot advances by more than this (default 20)
# SLOT_JUMP_THRESHOLD=20

# Optional: makes Solscan links match your cluster (devnet/testnet). Omit for mainnet.
# SOLSCAN_CLUSTER=devnet

# ---- Ethereum (EVM) ----
# Set to enable Ethereum watcher (e.g. mainnet, Base, Arbitrum)
# RPC_URL_ETH=https://eth.llamarpc.com
# RPC_URL_ETH=https://mainnet.base.org

# Optional: watch this Ethereum address (0x...) for incoming/outgoing txs
# WATCH_ADDRESS_ETH=0xYourEthereumAddress

# Optional: label used in logs/alerts (e.g. Base, Arbitrum, Ethereum)
# ETH_CHAIN_NAME=Ethereum

# Optional: block explorer URLs for Ethereum alerts (default: etherscan.io)
# ETH_EXPLORER_TX=https://etherscan.io/tx
# ETH_EXPLORER_BLOCK=https://etherscan.io/block
# For Base: https://basescan.org/tx and https://basescan.org/block

# Optional: alert when Ethereum block number jumps by more than this (default 20)
# ETH_BLOCK_JUMP_THRESHOLD=20

# ---- Alerts ----
# OpenClaw: where to send alerts (e.g. Telegram chat id, or phone for WhatsApp)
# OPENCLAW_ALERT_TARGET=123456789

# Optional: Discord/Slack webhook for testing without OpenClaw
# ALERT_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: poll interval in ms for all chains (default 15000)
# POLL_INTERVAL_MS=15000

# Optional: timeout (ms) for OpenClaw CLI + webhook requests (default 15000)
# NOTIFY_TIMEOUT_MS=15000
